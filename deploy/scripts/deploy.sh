# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

!# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

w# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

w# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

載# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

入# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

統# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

一# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

設# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

定# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檔# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

並# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

自# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

動# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

同# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

步# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

與# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

9# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

+# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

已# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

改# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

為# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

執# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

行# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

期# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

記# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

憶# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

體# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

動# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

態# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

注# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

入# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

，# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

不# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

再# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

覆# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

寫# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

磁# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

碟# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檔# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

案# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

解# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

析# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

命# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

令# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

列# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

參# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

支# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

援# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

靈# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

活# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

順# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

序# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

與# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

參# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

@# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

用# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

法# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

參# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

說# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

明# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

環# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

境# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

預# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

設# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

工# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

具# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

預# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

設# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

前# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

先# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

執# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

行# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

獨# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

置# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

腳# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

本# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

跳# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

過# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

映# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

像# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檔# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

置# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

預# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

設# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

若# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

指# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

定# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

為# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

，# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

直# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

接# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

委# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

派# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

至# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

專# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

屬# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

腳# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

本# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

@# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

6# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

尋# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

找# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

並# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

重# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

用# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

變# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

優# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

先# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

順# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

序# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

<# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

專# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

案# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

根# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

目# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

錄# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

偵# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

測# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

到# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

應# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

用# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

程# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

式# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

環# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

境# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檔# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

w# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

~# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

^# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

去# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

除# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

前# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

後# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

空# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

白# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

與# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

換# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

行# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

符# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

號# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

^# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

去# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

除# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

前# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

後# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

引# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

號# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

單# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

引# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

號# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

或# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

雙# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

引# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

號# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

~# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

^# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

~# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

^# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

<# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

自# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

動# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

萃# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

取# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

變# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

化# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

網# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

域# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

名# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

稱# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

3# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

,# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

+# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

程# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

序# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

參# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

化# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

管# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

理# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

環# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

境# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

命# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

名# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

空# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

間# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

工# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

具# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

執# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

行# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

映# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

像# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檔# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

置# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

預# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

期# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

對# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

外# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

網# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

域# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

基# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

礎# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

自# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

簽# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

3# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

獨# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

映# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

像# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檔# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

置# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

步# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

驟# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

可# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

單# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

獨# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

執# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

行# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

，# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

亦# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

可# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

由# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

參# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

觸# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

發# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

前# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

置# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

作# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

業# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

觸# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

發# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

獨# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

置# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

腳# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

本# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

映# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

像# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檔# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

置# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

與# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

推# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

送# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

完# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

成# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

，# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

繼# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

續# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

執# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

行# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

資# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

源# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

4# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檢# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

查# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

!# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

提# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

示# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

目# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

前# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

是# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

，# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

即# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

將# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

切# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

換# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

至# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

警# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

告# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

不# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

存# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

在# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

於# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

本# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

機# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

中# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

！# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

（# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

目# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

前# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

尚# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

未# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

叢# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

集# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

，# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

可# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

略# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

過# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

切# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

換# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

）# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

5# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

若# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

不# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

存# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

在# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

!# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

命# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

名# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

空# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

間# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

5# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檢# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

查# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

並# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

配# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

置# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

憑# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

證# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

避# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

免# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

!# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

j# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

j# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

%# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

%# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

複# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

製# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

現# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

有# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

至# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

*# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

># -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

&# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

6# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

重# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

用# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

變# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

同# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

步# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

3# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

3# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

（# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

中# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

未# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

指# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

定# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

，# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

自# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

動# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

產# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

生# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

一# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

組# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

安# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

全# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

金# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

鑰# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

）# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

3# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

3# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

更# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

新# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

7# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

動# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

態# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

同# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

步# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

建# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

立# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

更# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

新# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

5# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

4# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

J# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Q# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

J# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Q# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

4# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

8# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

執# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

行# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

或# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

正# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

在# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

使# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

用# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

動# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

態# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

注# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

入# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

環# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

境# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

變# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

數# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

網# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

域# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

、# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

、# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

憑# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

證# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

至# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

資# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

源# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

串# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

流# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

+# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

@# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

F# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

正# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

在# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

使# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

用# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

+# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

;# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

X# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

+# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

(# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

)# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

V# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

G# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

[# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

]# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

I# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

\# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

錯# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

誤# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

未# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

知# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

的# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

工# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

具# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

'# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

。# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

請# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

選# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

擇# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

z# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

或# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

。# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

x# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

9# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

等# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

待# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

與# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

檢# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

查# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

狀# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

態# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

## -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

等# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

待# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

D# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

.# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

/# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

1# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

2# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

0# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

|# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

部# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

署# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

完# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

成# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

！# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

狀# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

態# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

摘# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

要# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

：# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

k# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

u# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

b# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

d# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

,# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

,# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

v# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

,# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

g# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

,# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

f# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

n# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

N# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

M# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

-# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

連# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

線# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

位# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

址# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

:# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

$# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

{# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

L# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

U# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

O# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

S# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

T# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

R# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

A# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

P# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

_# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

E# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

Y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

}# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

（# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

首# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

次# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

連# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

線# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

請# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

進# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

入# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

網# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

頁# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

點# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

擊# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

C# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

l# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

i# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

m# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

H# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

，# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

並# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

輸# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

入# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

上# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

述# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

B# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

s# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

t# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

r# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

a# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

p# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

K# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

y# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

）# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

e# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

c# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

h# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

o# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

 # -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

=# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

"# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -


# -----------------------------------------------------------------------------
# 7. 動態建立 / 同步 ConfigMap (cocklebur-config)
# -----------------------------------------------------------------------------
OVERLAY_DIR="${ROOT_DIR}/deploy/k8s/overlays/${ENV}"

echo "建立 / 更新 ConfigMap 'cocklebur-config'..."
kubectl create configmap cocklebur-config \
  --namespace="$NAMESPACE" \
  --from-literal=POPUP_APP_MODE=server \
  --from-literal=POPUP_DATA_DIR=/data \
  --from-literal=POPUP_BASE_URL="${POPUP_BASE_URL}" \
  --from-literal=POPUP_MAX_UPLOAD_MB="${POPUP_MAX_UPLOAD_MB:-50}" \
  --from-literal=POPUP_IMPORT_MAX_MB="${POPUP_IMPORT_MAX_MB:-2048}" \
  --from-literal=POPUP_PROJECT_QUOTA_MB="${POPUP_PROJECT_QUOTA_MB:-1024}" \
  --dry-run=client -o yaml | kubectl apply -f -

