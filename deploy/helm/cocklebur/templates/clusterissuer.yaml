{{- if .Values.clusterIssuerCA.create -}}
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.clusterIssuerCA.name }}
  labels:
    {{- include "cocklebur.labels" . | nindent 4 }}
spec:
  ca:
    secretName: {{ .Values.clusterIssuerCA.caSecretName }}
{{- end }}
