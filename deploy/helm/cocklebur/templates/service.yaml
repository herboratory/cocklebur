apiVersion: v1
kind: Service
metadata:
  name: {{ include "cocklebur.fullname" . }}
  labels:
    {{- include "cocklebur.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cocklebur.selectorLabels" . | nindent 4 }}
